apiVersion: v1
kind: PersistentVolume
metadata:
  name: vllm-model-pv
spec:
  capacity:
    storage: 4Gi
  accessModes:
    - ReadOnlyMany
  storageClassName: hostpath
  hostPath:
    path: ${LLM_MODEL_DIR}
  persistentVolumeReclaimPolicy: Retain
